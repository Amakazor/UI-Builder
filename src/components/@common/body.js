import { backgroundColorProperty, textColorProperty } from '../properties/properties';

const body = {
    name: 'Body',
    nodes: ['body'],
    properties: [
        backgroundColorProperty,
        textColorProperty
    ]
};

export default body;